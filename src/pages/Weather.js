import { Container } from "react-bootstrap";

const Weather = () => {

    return (
        <Container>
            <h1>This is the Weather Page.</h1>
        </Container>
    );
};

export default Weather;
